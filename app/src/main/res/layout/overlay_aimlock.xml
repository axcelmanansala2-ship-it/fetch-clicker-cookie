<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="90dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/colorOverlayBg"
    android:padding="5dp">

    <Button
        android:id="@+id/btnAimToggle"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:text="START"
        android:textColor="@color/colorPrimary"
        android:textSize="12sp"
        android:textStyle="bold"
        android:backgroundTint="@color/colorAimLockStart"
        android:stateListAnimator="@null" />

    <TextView
        android:id="@+id/tvAimStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="#FFFFFF"
        android:textSize="8sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:paddingTop="2dp" />

    <TextView
        android:id="@+id/tvAimDetail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="@color/colorAimLockStart"
        android:textSize="8sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end" />

</LinearLayout>
